global:
  scrape_interval: 15s
scrape_configs:
  - job_name: sonarqube
    metrics_path: /api/monitoring/metrics
    bearer_token: prometheusPassword0!
    static_configs:
      - targets: ["sonarqube:9000"]
